[project]
name = "fouruna"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Kijoon Jeong", email = "jkijoon701@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
fouruna = "fouruna:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.2",
]
