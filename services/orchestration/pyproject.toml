[project]
name = "orchestration"
version = "0.1.0"
description = "Orchestrates DE4 data workflows"
requires-python = ">=3.12,<3.13"
dependencies = ["de4-core"]

[project.scripts]
orchestration = "orchestration:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
