[project]
name = "dashboard"
version = "0.1.0"
description = "Visualizes ride-comfort data"
requires-python = ">=3.12,<3.13"
dependencies = ["de4-core"]

[project.scripts]
dashboard = "dashboard:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
