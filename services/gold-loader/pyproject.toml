[project]
name = "gold-loader"
version = "0.1.0"
description = "Loads curated ride-comfort data"
requires-python = ">=3.12,<3.13"
dependencies = ["de4-core"]

[project.scripts]
gold-loader = "gold_loader:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
